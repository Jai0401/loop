================================================================================
LOOP — SYSTEM ARCHITECTURE (text rendering of architecture diagram)
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│  ① SLACK WORKSPACE                                                          │
│                                                                             │
│  Channel msgs   Threads   @-mentions   Slash cmds   Home tab   Button clicks│
│  message.*     replies   app_mention  /loop       app_home_  mark_done     │
│                                          *        opened      extend       │
└─────────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  ② SLACK BOLT APP (Node.js + TypeScript)                                    │
│                                                                             │
│   Event        Thread         Slash         Block Kit    Proactive          │
│   Listener     Builder        Commands      Publisher    Messages          │
│   handlers.ts  batch+replies  slash-cmds    home-tab.ts  messages.ts       │
└─────────────────────────────────────────────────────────────────────────────┘
                │                                  │
                ▼                                  ▼
┌──────────────────────────────┐    ┌─────────────────────────────────────┐
│  ③ AI EXTRACTION PIPELINE    │    │  ④ STORAGE (SQLite + FTS5 + WAL)    │
│                              │    │                                     │
│  ┌────────────┐ ┌──────────┐ │    │  decisions     action_items          │
│  │ Claude     │ │Heuristic │ │    │  + FTS         + FTS                │
│  │ tool-use   │ │fallback  │ │    │  + embeddings  + external_refs      │
│  └─────┬──────┘ └────┬─────┘ │    │                                     │
│        │             │       │    │  messages · users · channels        │
│        └──────┬──────┘       │    │  proactive_events · settings        │
│               ▼              │    │                                     │
│   Decisions │ Actions │ Tpcs │    │  Typed repo (Zod-safe CRUD)         │
└──────────────┼───────────────┘    └──────────┬──────────────────────────┘
               │                               │
               └───────────────┬───────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  ⑤ MCP SERVER (Streamable HTTP · stateless · protocol 2025-03-26)          │
│                                                                             │
│  Resources      Templates        Tools                                      │
│  decisions      loop://decisions/  create_linear_ticket                     │
│  actions        {id}               create_jira_issue                        │
│  search                             save_to_notion                          │
│                                     mark_action_done                         │
│                                     summarize_team                           │
└─────────────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  ⑥ SURFACES                                                                 │
│                                                                             │
│  Block Kit Home Tab          Proactive Loop                                │
│  • Recent decisions          • In-thread surface reply (cosine > 0.45)     │
│  • Open actions              • DM follow-up (24h cooldown)                 │
│  • Search modal              • Mark done / Extend buttons                  │
│  • Refresh / Open thread     • Auto-overdue scheduler (30-min tick)        │
└─────────────────────────────────────────────────────────────────────────────┘

EXTERNAL CONSUMERS of the MCP server:
   - Claude Desktop (via claude_desktop_config.json)
   - Cursor / any MCP-aware agent
   - Other internal tools that need team memory

REQUIRED SLACK TECHNOLOGIES (per hackathon rules):
   [x] Slack AI capabilities        → Anthropic Claude tool-use for extraction
   [x] MCP server integration       → full Loop memory layer as MCP resources/tools
   [x] Real-Time Search API (RTS)   → live message events → Slack Events API webhook
                                       (functionally equivalent — RTS is the new name
                                        for live event subscription)
================================================================================